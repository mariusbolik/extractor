# Used by Wrangler only; never exposed to the Worker runtime.
CLOUDFLARE_API_KEY=

# Shared server-to-server credential. Use the same value as LLMBase's
# EXTRACTR_SERVICE_TOKEN and configure it as a Worker secret in production.
LLMBASE_SERVICE_TOKEN=
